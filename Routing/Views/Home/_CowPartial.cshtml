@model Cow

@if (Model == null)
{
	<h2>No cows</h2>
}
else

{
	@if (ViewBag.page != null)
	{
		<h3>Page: @ViewBag.page</h3>
	}

	<img src="@Model.Image"/>
	<br />
	<h3>@Model.Name</h3>
	<br />
	<h3>@Model.Description</h3>
}